# 模块拆分清单 — 2026-08-19 客户端下载页

本次闭环的模块边界。新功能另建 `pipeline/work/YYYY-MM-DD-短标题/`，不要往本表追加。

## 本次目标

1. 走六阶段：brainstorming → 人批 spec → 实现 → `gate` / `deploy` / `accept` → 钉钉
2. 方案：93 独立 nginx（443 + 80 跳转）静态站 `/genlotvpn/`；部署扫 `apps/` 写清单；按操作系统默认平台、中/EN
3. **不改** 9443 / vpn-auth 业务、不反代 9400、不进 92 管理端登录
4. 客户端要编的包仍放 `apps/`；本条不改桌面端源码，不跑 `client-ref`，不声称客户端已验收

## 模块清单

| 编号 | 模块名称 | 技术栈 | 熟悉度 | 优先级 | 依赖 | 验收标准 | 安全级 | 状态 |
|------|----------|--------|--------|------|----------|--------|------|
| M1 | 下载站页面 + 中/英手册 | 静态 HTML/CSS/JS | 熟悉 | P0 | `manifest.json`、本地 Logo | 打开 `/genlotvpn/download/` 见标题 **客户端下载**；可切 EN、可切平台、手册二级可开 | S2 | 已验收 |
| M2 | 93 下载 nginx | Docker / nginx / 本机证书 | 熟悉 | P0 | 网络放行该公网 IP 的 80/443 | 443 出静态；80 跳 443；关目录浏览；不反代 9400/9443 | S1 | 内网上线 |
| M3 | 扫 `apps/` 部署同步 | Python / `deploy.py vpn` | 熟悉 | P0 | M1、M2、`apps/` 约定文件名 | 部署后清单含当前最高版本；有包则可下载 | S2 | 已验收 |

对照：[spec.md](./spec.md)。跨机托管见 [_shared/ADR-006-93下载站.md](../_shared/ADR-006-93下载站.md)。

## 里程碑

| 周次 | 里程碑 | 包含模块 | 交付物 |
|------|--------|----------|--------|
| 闭环演示 | 外网/该 IP 可打开下载页并下到当前包 | M1–M3 | spec + 门禁 + 下载页 accept |

## 风险识别

| 风险 | 影响 | 应对 |
|------|------|------|
| 未批 spec 就开写 | 返工 | AGENTS.md HARD-GATE |
| nginx 误反代 vpn-auth | 认证服务敞到公网 | 配置只静态；验收检查不应出现 9400 反代 |
| 公网 IP 用自签证书 | 浏览器告警 | 本期接受；spec 写明；accept 忽略校验 |
| 443 未由网络映到 93 | 外网打不开 | 人配合网络；脚本不假装公网已通 |
| 把本条塞进管理端 accept | 会去 92 登录 | 新 `accept.py download`，免登录 |
