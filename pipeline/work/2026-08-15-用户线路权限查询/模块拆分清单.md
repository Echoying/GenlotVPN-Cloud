# 模块拆分清单 — 2026-08-15 用户线路权限查询

本次闭环的模块边界。新功能另建 `pipeline/work/YYYY-MM-DD-短标题/`，不要往本表追加。

## 本次目标

1. 走六阶段：brainstorming → 人批 spec → 实现 → `gate` / `deploy` / `accept` → 钉钉
2. 本条只改管理端查询；不编客户端、不改易安联写授权
3. 客户端验收仍人工勾 PHASE2，不纳入自动 accept

## 模块清单

| 编号 | 模块名称 | 技术栈 | 熟悉度 | 优先级 | 依赖 | 验收标准 | 安全级 | 状态 |
|------|----------|--------|--------|--------|------|----------|--------|------|
| M1 | 用户线路权限汇总 API | Java / yianlian | 熟悉 | P0 | `IVpnLineAuthService`、`Yal*Auth`、线路用户映射 | `GET .../line-auths` 按线路返回应用组/应用/来源；不改 `authorized-lines` | S2 | 已验收 |
| M2 | 列表「查看权限」弹窗 | Vue2 / `views/vpn/user`、`views/vpn/local/user` | 熟悉 | P0 | M1 | 两个列表能打开「线路权限」弹窗，Playwright 按 spec 断言 | S2 | 已验收 |
| M3 | 查看权限按钮权限 | `sys_menu` F + `v-hasPermi` | 熟悉 | P0 | M2 | `yianlian:user:queryAuth` / `vpn:localUser:queryAuth`；无权限按钮不可见 | S2 | 已验收 |

**未批 spec 前不写码。** spec 见 [spec.md](./spec.md)。

## 里程碑

| 周次 | 里程碑 | 包含模块 | 交付物 |
|------|--------|----------|--------|
| 闭环演示 | 首条管理端查询可验收 | M1–M3 | spec + 门禁 + `验收报告/` + 钉钉 |

## 风险识别

| 风险 | 影响 | 应对 |
|------|------|------|
| 未批 spec 就开写 | 返工、一票否决「AI 结果无人审」 | AGENTS.md HARD-GATE |
| 把流水线绑死在某一页 | 后面功能无法复用 | accept 参数来自当前 spec |
| 把客户端塞进自动验收 | 安全项被跳过 | 客户端只走 PHASE2_CHECKLIST |
| 凭证入库或进对话 | 合规否决 | 环境变量 + 脱敏 Rule |
