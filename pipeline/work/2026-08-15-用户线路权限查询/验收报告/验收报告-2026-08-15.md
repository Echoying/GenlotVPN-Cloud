# 验收报告 2026-08-15：用户线路权限查询

| 项 | 内容 |
|----|------|
| Spec | [spec-2026-08-15-用户线路权限查询.md](../spec-2026-08-15-用户线路权限查询.md) |
| 计划 | [plan-2026-08-15-用户线路权限查询.md](../plan-2026-08-15-用户线路权限查询.md) |
| 模块 | M1 汇总 API / M2 弹窗 / M3 按钮权限 |
| 安全级 | S2（只读管理端查询；本条不编客户端、不走人验 PHASE2） |
| 环境 | 管理端 http://10.27.0.92 ；业务库 91 `ry-cloud`；账号 系统管理员（admin） |
| 最终结果 | **通过**（回修轮次 2） |
| 门禁表 | [质量门禁记录.md](../质量门禁记录.md) |

本报告由 `bin/accept_admin.py` 两次通过记录 + 人工对照截图 / spec 整理。脚本会覆盖同日文件，故把两条场景和门禁过程写在同一份里。

---

## 1. 验收结论

两条 spec 第 7 节页面路径均已打开弹窗，标题为 `线路权限 - {userName}`，内容按线路分组，来源文案为 `部门` / `角色` / `用户`。admin 能看到「查看权限」。

未覆盖（见第 7 节）：空权限文案「暂无线路权限」、无权限账号按钮隐藏、接口 JSON 抓包、用户不存在时的失败提示。

---

## 2. 门禁过程

顺序：`gate` → `deploy` → 91 菜单 SQL → 两次 `accept`。本条只用 `gate.bat server`（不编客户端）。

| 步骤 | 命令 / 动作 | 证据 | 结果 |
|------|-------------|------|------|
| 构建 Java | `bin\gate.bat server` | Maven `BUILD SUCCESS`（约 18:24） | 通过 |
| 构建前端 | 同上；首次因 bat 延迟展开误报失败后 `bin\gate.bat ui` | `[gate] 通过`，exit 0 | 通过 |
| 部署 92 | 首次 `bin\deploy.bat` 因 `CALL deploy-admin-92.bat` 递归失败；改为 `python bin\deploy_admin_92.py` | `[deploy] 完成`，exit 0（约 18:37） | 通过 |
| 注入运行中 jar | 宿主机与容器 jar MD5 不一致（匿名卷 `/home/ruoyi`）；`docker cp` + `docker restart ruoyi-modules-yianlian` | 注入后两边 MD5 均为 `2025faa3…` | 通过 |
| 菜单 | 91 MySQL 执行 `sql/update/20260815_line_auth_query_menu.sql` | `1087 yianlian:user:queryAuth`、`1088 vpn:localUser:queryAuth`，`sys_role_menu` role_id=1 | 通过 |
| 线路用户 accept | 见 4.1 | 轮次 2 通过 | 通过 |
| 本地用户 accept | 见 4.2 | 轮次 2 通过 | 通过 |
| 钉钉 | 首次 token 误填整段 webhook，接口 `token is not exist`；拆分后补发 | `{"status":"sent"}` | 已发出 |

`1085` / `1086` 已被同步任务占用，本条菜单改为 **1087 / 1088**。父菜单仍是 1065（线路用户）、1072（本地用户），与 spec 第 6 节一致。

---

## 3. Spec 覆盖核对

| Spec | 要求 | 本次怎么验 | 结果 |
|------|------|------------|------|
| 3.1 线路用户入口 | 更多 → 查看权限，与「授权」并列 | 截图操作列有修改/删除/更多；下拉含查看权限 | 通过 |
| 3.1 标题 | `线路权限 - {userName}` | 弹窗标题 `线路权限 - zhujunxiong` | 通过 |
| 3.1 按线路 + 来源合并 | 分组；来源如 `部门, 角色` 不重复铺行 | 见 4.1 / 4.2 实测行 | 通过 |
| 3.1 空权限 / 用户不存在 | 「暂无线路权限」；不存在则失败不弹空窗 | **未测**（所选用户都有数据） | 未覆盖 |
| 3.2 本地用户入口 | 操作列「查看权限」 | 截图每行有「查看权限」 | 通过 |
| 3.2 按已同步线路展开 | 多线路分别列出 | 本地用户弹窗同时有「穗彩开发环境」「South_Africa」 | 通过 |
| 3.3 名称解析 | 组名 / 应用名 / 线路名；解析不到显示原始 ID | 组「热线」「PDC」、应用「BDC」「PDC」「OPS_20203」；South_Africa 有一行应用为 `2`（按 spec 回退原始 ID） | 部分：回退出现，未核对库内是否缺名称 |
| 3.3 来源文案 | 仅 `部门` `角色` `用户` | 截图无其它字样 | 通过 |
| 4 两条 GET、非 Inner | `/vpn/user/{id}/line-auths`、`/vpn/local/user/{id}/line-auths` | 前端已调；注入新 jar 后弹窗有数据（此前 HTTP 404） | 通过（未抓响应 JSON） |
| 4 禁止 spaKey | 弹窗列只有应用组/应用/来源 | 截图无密钥字段 | 通过（页面级） |
| 5 权限指令 | `yianlian:user:queryAuth` / `vpn:localUser:queryAuth` | admin 能见按钮；菜单已挂 role_id=1 | 通过（未测无权限账号） |
| 6 菜单 F | 不新建 C 页 | 无新侧栏项，只加按钮 | 通过 |
| 7 accept 路径/文案 | 两 path + 点击 + 「线路权限」 | 见第 4 节命令 | 通过 |
| 8 安全 | 不关桌面/选线验证码；报告脱敏 | 只关 92 管理端登录验证码；报告无密码/token | 通过 |
| 9 非目标 | 不导出、不反查、不写授权、不写易安联 | 未做这些 | 遵守 |

---

## 4. 页面验收明细

前置：92 两条列表均有用户；admin 重新登录后带上新按钮权限。Playwright 登录页验证码框已卸掉（92 Nacos `security.captcha.enabled`）。

### 4.1 线路用户

```
python -u bin\accept_admin.py --path /yianlian/vpn/user --click 更多 --click 查看权限 --expect-text 线路权限 --title 线路用户线路权限 --module M2 --round 2
```

| 项 | 值 |
|----|----|
| 打开 | http://10.27.0.92/yianlian/vpn/user |
| 时间 | 2026-08-15T18:54:18 |
| 脚本结果 | 通过（exit 0） |
| 截图 | `docs/work/验收报告/screenshot-20260815-185407.png` |
| 列表线路筛选 | 德彩开发环境 |
| 点中行 | 用户编号 1，`zhujunxiong` / 朱军雄，部门国内业务部 |

| 断言 | 结果 |
|------|------|
| 登录页无验证码 | 通过 |
| 登录成功进入后台 | 通过 |
| 打开 /yianlian/vpn/user | 通过 |
| 点击「更多」 | 通过 |
| 点击「查看权限」 | 通过 |
| 弹窗标题含「线路权限」 | 通过 |

**弹窗实测（对照截图，不是脚本自动断言）：**

- 标题：`线路权限 - zhujunxiong`
- 分组：仅 **穗彩开发环境**（与列表筛选的「德彩开发环境」不是同一条线路名，说明汇总按授权线路列出，不是只显示当前筛选线路）
- 表列：应用组 / 应用 / 来源

| 应用组 | 应用 | 来源 |
|--------|------|------|
| 热线 | （空） | 角色, 用户 |

应用列为空符合「只授了应用组、未授具体应用」的展示；来源合并为 `角色, 用户`，未拆成两行。

### 4.2 本地用户

```
python -u bin\accept_admin.py --path /yianlian/local/user --click 查看权限 --expect-text 线路权限 --title 本地用户线路权限 --module M2 --round 2
```

| 项 | 值 |
|----|----|
| 打开 | http://10.27.0.92/yianlian/local/user |
| 时间 | 2026-08-15T18:55:05 |
| 脚本结果 | 通过（exit 0） |
| 截图 | `docs/work/验收报告/screenshot-20260815-185500.png` |
| 点中行 | `zhujunxiong`（操作列可见：修改 / 删除 / 同步 / 离线登录 / **查看权限** / 更多） |

| 断言 | 结果 |
|------|------|
| 登录页无验证码 | 通过 |
| 登录成功进入后台 | 通过 |
| 打开 /yianlian/local/user | 通过 |
| 点击「查看权限」 | 通过 |
| 弹窗标题含「线路权限」 | 通过 |

**弹窗实测：**

- 标题：`线路权限 - zhujunxiong`
- 两条线路，说明已按 `localUserId` 展开同步出的线路用户后再汇总

**穗彩开发环境**

| 应用组 | 应用 | 来源 |
|--------|------|------|
| 热线 | （空） | 角色, 用户 |

与线路用户页同一用户在该线路上的结果一致。

**South_Africa**

| 应用组 | 应用 | 来源 |
|--------|------|------|
| PDC | BDC | 部门 |
| PDC | PDC | 部门 |
| （空） | OPS_20203 | 用户 |
| （空） | 2 | 用户 |
| （空） | PDC | 用户 |
| （空） | BDC | 用户 |

来源只有 spec 允许的三种。应用 `2` 为名称解析回退（spec 3.3）；未在本报告中核对 `vpn_service` 是否缺这条名称。

---

## 5. 回修记录（2 轮）

| 轮次 | 现象 | 根因 | 处理 |
|------|------|------|------|
| 0 | 点不到「查看权限」；截图里下拉其实已打开且有该文案 | Playwright `get_by_text().first` 点到表格里 Element UI 隐藏副本 | `accept_admin.py` 只点可见节点 |
| 1 | 脚本误报通过；截图有「系统接口404异常」「加载线路权限失败」 | ① expect-text 模糊匹配 toast 里的「线路权限」② 容器匿名卷挡住新 jar，运行中仍是旧包 | 断言改为弹窗标题；`docker cp` 新 jar 并重启；`deploy_admin_92.py` 增加注入步骤 |
| 2 | 注入后立刻跑时点不到「更多」（表未刷出）；稍后重跑 | yianlian 刚重启，列表未就绪 | 等服务起来后再 accept；脚本增加等表格 |

中间还修了流水线本身（不改变业务口径）：`gate-admin.bat` 延迟展开、`deploy.bat`/`gate.bat`/`accept.bat` 避免 Windows `CALL foo-bar.bat` 递归。后续已写入 [ADR-003](../ADR-003-流水线踩坑.md)，并去掉 Java `VOLUME /home/ruoyi`、compose 绑定 jar、accept 追加报告与失败自动钉钉。

失败截图（勿当最终通过）：`screenshot-20260815-184016.png`（下拉已开）、`screenshot-20260815-184341.png`（404 toast）。

---

## 6. 安全与脱敏

- 接口需登录，权限标识见第 3 节；不是 `@InnerAuth`，未改 `authorized-lines`。
- 弹窗未见 `spaKey`、密码、`encryptedPwd`。
- 本报告不写 SSH / 库 / 管理端 / 钉钉凭证。
- 未关闭桌面端、选线验证码。

---

## 7. 未覆盖与残留

| 项 | 说明 |
|----|------|
| 「暂无线路权限」 | spec 3.1 / 3.2 空数据文案，未找无授权用户测 |
| 用户不存在 | 未测错误提示、不弹空成功窗 |
| 无权限账号 | 未用非 admin 确认按钮隐藏 |
| 接口 JSON | 未保存 `data.userType` / `lines[]` 原始响应 |
| 应用名 `2` | 回退 ID，建议事后对 `vpn_service` 补名称或查授权脏数据 |
| 客户端 | 本条不编、不自动验收 |

以上不影响 spec 第 7 节两条 accept 通过，但完整产品验收仍缺空态与权限反例。
