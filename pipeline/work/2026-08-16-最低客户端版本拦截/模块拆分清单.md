# 模块拆分清单 — 2026-08-16 最低客户端版本拦截

本次闭环的模块边界。新功能另建 `pipeline/work/YYYY-MM-DD-短标题/`，不要往本表追加。

## 本次目标

1. 走六阶段：brainstorming → 人批 spec → 实现 → `gate` / `deploy` / `accept` → 钉钉
2. 方案：yianlian 独立策略表 + vpn-auth 云端登录硬拦 + 客户端失败弹窗（文案 + 分平台下载链接）
3. 拦截的是桌面端**云端登录页**（`LOGIN` / `ruoyi-vpn-auth`），**不拦**本地控制器 / 易安联 SDK 登录
4. 客户端要编、要打包，验收仍人工勾 PHASE2，不纳入自动 accept

## 模块清单

| 编号 | 模块名称 | 技术栈 | 熟悉度 | 优先级 | 依赖 | 验收标准 | 安全级 | 状态 |
|------|----------|--------|--------|--------|------|----------|--------|------|
| M1 | 版本策略表 + 管理 API/页 | Java / yianlian + Vue + SQL | 熟悉 | P0 | 单行表 `vpn_client_version_policy`、菜单 | 超管打开「客户端版本策略」可开关、保存最低版本与 Win/Mac 链接 | S2 | 已验收 |
| M2 | vpn-auth 登录硬拦 | Java / vpn-auth + proto + Feign | 熟悉 | P0 | M1、内部读策略接口 | 开启后低/缺版本登录 `code=426`；关闭后放行；不改 TLS | S1 | 已实现，待人审 |
| M3 | 桌面端上报 + 失败弹窗 | Qt / ruoyi-vpn-client | 熟悉 | P0 | M2、`AppInfo.version` | 登录失败弹窗含升级文案与可点下载链接；不拦 SDK | S1 | 已实现，待 PHASE2 |

**spec 已批准。** 见 [spec.md](./spec.md)。M1 已 accept；M2/M3 待人验。

## 里程碑

| 周次 | 里程碑 | 包含模块 | 交付物 |
|------|--------|----------|--------|
| 闭环演示 | 策略可配 + 云端登录可拦 + 客户端可提示 | M1–M3 | spec + 门禁 + 管理端验收 + PHASE2 人勾 |

## 风险识别

| 风险 | 影响 | 应对 |
|------|------|------|
| 未批 spec 就开写 | 返工 | AGENTS.md HARD-GATE |
| 误拦易安联 SDK 登录 | 连线流程中断 | 范围写死只改云端 `LOGIN` |
| 开启拦截但旧客户端不带版本字段 | 全部无法登录 | 符合预期；默认 `enabled=0` 灰度 |
| 把客户端塞进自动验收 | 安全项被跳过 | M3 只走 PHASE2 |
| 凭证入库或进对话 | 合规否决 | 环境变量 + 脱敏 |
