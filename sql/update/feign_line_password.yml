# Feign 线路密码轮换超时（合并到 application-dev.yml）
# 易安联改密含代理会话登录 + token + reset，实测可达 15s 以上；
# 须满足：readTimeout > LinePasswordRotationService.LOCK_WAIT_MS（45s）
feign:
  client:
    config:
      remoteVpnLinePasswordService:
        connectTimeout: 10000
        readTimeout: 60000
