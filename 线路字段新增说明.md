# 线路管理字段新增说明

## 变更概述

本次变更为 `line_app` 表新增了4个必填字段，并调整了密钥字段的处理逻辑。

## 数据库变更

### 新增字段

执行SQL脚本：`sql/update/add_line_fields.sql`

```sql
ALTER TABLE line_app
  ADD COLUMN host VARCHAR(255) NOT NULL COMMENT '服务器域名或IP' AFTER url,
  ADD COLUMN srv_port INT NOT NULL COMMENT '服务器端口号' AFTER host,
  ADD COLUMN spa_port INT NOT NULL COMMENT '敲门端口' AFTER srv_port,
  ADD COLUMN spa_key VARCHAR(32) NOT NULL COMMENT '预共享秘钥（MD5加密32位小写）' AFTER spa_port;
```

### 字段说明

| 字段名 | 类型 | 说明 | 备注 |
|--------|------|------|------|
| host | VARCHAR(255) | 服务器域名或IP | 必填 |
| srv_port | INT | 服务器端口号 | 必填，范围 1-65535 |
| spa_port | INT | 敲门端口 | 必填，范围 1-65535 |
| spa_key | VARCHAR(32) | 预共享秘钥 | 必填，MD5加密32位小写存储 |

## 后端变更

### 1. 实体类 (LineApp.java)

新增4个字段属性：
- `private String host;` - 服务器域名或IP
- `private Integer srvPort;` - 服务器端口号
- `private Integer spaPort;` - 敲门端口
- `private String spaKey;` - 预共享秘钥

### 2. Mapper XML (LineAppMapper.xml)

- **resultMap**: 添加新字段映射
- **selectLineAppVo**: 查询语句包含新字段
- **insert**: 动态插入新字段
- **update**: 动态更新新字段（仅当非空时更新）

### 3. Service层 (VpnLineAppServiceImpl.java)

#### 密钥加密逻辑

**新增时**：
- `appSecret` 和 `spaKey` 均进行 MD5 加密（32位小写）
- 使用 `DigestUtils.md5DigestAsHex()` 方法

**编辑时**：
- 如果字段为空，则设置为 `null`（Mapper不会更新，保持原值）
- 如果字段有值，则进行 MD5 加密后更新

```java
// 新增时
if (StringUtils.isNotEmpty(lineApp.getAppSecret())) {
    lineApp.setAppSecret(encryptSpaKey(lineApp.getAppSecret()));
}
if (StringUtils.isNotEmpty(lineApp.getSpaKey())) {
    lineApp.setSpaKey(encryptSpaKey(lineApp.getSpaKey()));
}

// 编辑时
if (StringUtils.isEmpty(lineApp.getAppSecret())) {
    lineApp.setAppSecret(null);  // 不修改
} else {
    lineApp.setAppSecret(encryptSpaKey(lineApp.getAppSecret()));
}
if (StringUtils.isEmpty(lineApp.getSpaKey())) {
    lineApp.setSpaKey(null);  // 不修改
} else {
    lineApp.setSpaKey(encryptSpaKey(lineApp.getSpaKey()));
}
```

## 前端变更 (index.vue)

### 1. 列表展示

新增4列：
- 服务器 (host)
- 服务器端口 (srvPort)
- 敲门端口 (spaPort)
- 预共享秘钥 (spaKey)

"线路URL" 改为 "管理系统URL"

### 2. 表单布局

**调整为两列布局**（el-row + el-col :span="12"）：

```
第一行：线路名称 | 线路ID
第二行：密钥 | 管理系统URL
第三行：服务器 | 预共享秘钥
第四行：服务器端口 | 敲门端口
第五行：状态 | 备注
```

### 3. 表单字段

#### 管理系统URL
- 改为**必填**字段
- 校验规则：`{required: true, message: "管理系统URL不能为空", trigger: "blur"}`

#### 端口字段
- **placeholder** 显示范围：`"范围：1 - 65535"`
- 类型：`type="number"`
- 双向绑定：`v-model.number`
- 校验规则：
  ```javascript
  {
    required: true,
    message: "端口号不能为空",
    trigger: "blur"
  },
  {
    type: 'number',
    min: 1,
    max: 65535,
    message: "端口范围1-65535",
    trigger: "blur"
  }
  ```

#### 密钥字段 (appSecret 和 spaKey)

**输入框**：
- 类型：`type="password"`
- 显示密码切换：`show-password`
- placeholder：
  - 新增时：`"请输入密钥"` / `"请输入预共享秘钥"`
  - 编辑时：`"留空则不修改"`

**校验规则**：
```javascript
// 动态校验：新增时必填，编辑时可选
validateAppSecret(rule, value, callback) {
  if (!this.isEdit && !value) {
    callback(new Error('线路密钥不能为空'))
  } else {
    callback()
  }
}

validateSpaKey(rule, value, callback) {
  if (!this.isEdit && !value) {
    callback(new Error('预共享秘钥不能为空'))
  } else {
    callback()
  }
}
```

**编辑时处理**：
```javascript
// 获取数据后清空密钥字段，避免显示MD5密文
handleUpdate(row) {
  getLineApp(appId).then(response => {
    this.form = response.data
    this.form.appSecret = ''  // 清空
    this.form.spaKey = ''     // 清空
    this.isEdit = true
  })
}

// 提交时，空值从数据中移除（后端会保持原值）
submitForm() {
  if (this.isEdit) {
    const submitData = {...this.form}
    if (!submitData.appSecret) {
      delete submitData.appSecret
    }
    if (!submitData.spaKey) {
      delete submitData.spaKey
    }
    updateLineApp(submitData).then(...)
  }
}
```

## 功能特性

### 密钥安全性

1. **后端加密**：所有密钥字段（appSecret、spaKey）在后端使用 MD5 加密，前端不处理加密逻辑
2. **编辑保护**：编辑时不显示已加密的密文，用户留空则保持原密钥不变
3. **存储格式**：MD5 加密后为32位小写字符串

### 用户体验

1. **两列布局**：新增/编辑表单采用两列布局，提高空间利用率
2. **端口提示**：端口输入框灰色显示可填写范围（1-65535）
3. **必填提示**：管理系统URL 改为必填，与其他核心字段保持一致
4. **密码隐藏**：密钥字段使用密码框，支持显示/隐藏切换

## 测试要点

### 新增线路
- [ ] 所有必填字段验证生效
- [ ] 端口范围验证（1-65535）
- [ ] appSecret 和 spaKey 正确加密存储
- [ ] 数据库中密钥字段为32位小写MD5值

### 编辑线路
- [ ] 密钥字段显示为空（不显示MD5密文）
- [ ] 留空密钥字段，提交后原密钥保持不变
- [ ] 输入新密钥，提交后正确加密更新
- [ ] 其他字段正常更新

### 列表展示
- [ ] 新增4个字段列正常显示
- [ ] "管理系统URL" 列名正确显示
- [ ] 密钥字段显示为MD5密文（32位小写）

### Redis缓存
- [ ] 新增线路后缓存正确
- [ ] 编辑线路后缓存失效并重新加载
- [ ] 删除线路后缓存清除

## 部署步骤

1. **数据库迁移**：执行 `sql/update/add_line_fields.sql`
2. **后端部署**：
   - 编译 `ruoyi-modules-yianlian` 模块
   - 重启 yianlian 服务
3. **前端部署**：
   - 构建前端项目
   - 部署静态资源
4. **验证**：按测试要点逐项验证功能

## 注意事项

1. **数据库迁移**：执行SQL前请备份数据库
2. **已有数据**：新增字段为 NOT NULL，执行前需确保表为空或手动处理已有数据
3. **缓存清理**：部署后建议清理 Redis 中的 LineApp 缓存
4. **密钥安全**：MD5 为单向加密，无法解密，请妥善保管原始密钥
