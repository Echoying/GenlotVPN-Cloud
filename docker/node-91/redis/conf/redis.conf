# requirepass 123456

# 持久化目录（映射到宿主机 ./redis/data，启动前须 chmod/chown 保证可写）
dir /data

# 挂载目录权限异常时，避免因 RDB 失败阻断全部写操作（会话/令牌等）
stop-writes-on-bgsave-error no

# 日志输出到 stdout，由 docker logs 采集
loglevel notice
