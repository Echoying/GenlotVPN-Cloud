<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.yianlian.mapper.VpnFeedbackMapper">

    <resultMap type="VpnFeedback" id="VpnFeedbackResult">
        <id     property="id"              column="id"               />
        <result property="title"           column="title"            />
        <result property="content"         column="content"          />
        <result property="category"        column="category"         />
        <result property="status"          column="status"           />
        <result property="userId"          column="user_id"          />
        <result property="userName"        column="user_name"        />
        <result property="clientVersion"   column="client_version"   />
        <result property="clientPlatform"  column="client_platform"  />
        <result property="ipaddr"          column="ipaddr"           />
        <result property="imageUrls"       column="image_urls"       />
        <result property="createBy"        column="create_by"        />
        <result property="createTime"      column="create_time"      />
        <result property="updateBy"        column="update_by"        />
        <result property="updateTime"      column="update_time"      />
        <result property="remark"          column="remark"           />
    </resultMap>

    <sql id="selectFeedbackVo">
        select id, title, content, category, status, user_id, user_name,
               client_version, client_platform, ipaddr, image_urls,
               create_by, create_time, update_by, update_time, remark
        from vpn_feedback
    </sql>

    <select id="selectFeedbackList" parameterType="VpnFeedback" resultMap="VpnFeedbackResult">
        <include refid="selectFeedbackVo"/>
        <where>
            <if test="userName != null and userName != ''">
                AND user_name like concat('%', #{userName}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
        </where>
        order by id desc
    </select>

    <select id="selectFeedbackById" parameterType="Long" resultMap="VpnFeedbackResult">
        <include refid="selectFeedbackVo"/>
        where id = #{id}
    </select>

    <insert id="insertFeedback" parameterType="VpnFeedback" useGeneratedKeys="true" keyProperty="id">
        insert into vpn_feedback (
            title, content, category, status, user_id, user_name,
            client_version, client_platform, ipaddr, image_urls, create_time
        ) values (
            #{title}, #{content}, #{category},
            <choose>
                <when test="status != null and status != ''">#{status}</when>
                <otherwise>'0'</otherwise>
            </choose>,
            #{userId}, #{userName}, #{clientVersion}, #{clientPlatform}, #{ipaddr},
            <choose>
                <when test="imageUrls != null and imageUrls != ''">#{imageUrls}</when>
                <otherwise>'[]'</otherwise>
            </choose>,
            #{createTime}
        )
    </insert>

    <update id="updateStatus">
        update vpn_feedback
        set status = #{status}, update_time = sysdate()
        where id = #{id}
    </update>

</mapper>
