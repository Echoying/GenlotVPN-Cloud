# Tomcat
server:
  port: 9205

# VPN AES密码加密配置
vpn:
  aes:
    key: EnSwordAgent@123
    iv: 321@tnegAdrowSnE
  offline:
    hmac:
      key: "1^z4UGNHULekb*_msnrJZUpIJC.H_*.Npw22NJ9Vtv2_Yqro"

# Spring
spring:
  application:
    # 应用名称
    name: ruoyi-yianlian
  profiles:
    # 环境配置
    active: dev
  cloud:
    nacos:
      discovery:
        # 服务注册地址
        server-addr: ruoyi-nacos:8848
      config:
        # 配置中心地址
        server-addr: ruoyi-nacos:8848
        # 配置文件格式
        file-extension: yml
        # 共享配置
        shared-configs:
          - application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
